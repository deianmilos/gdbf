
# Collection policy:
# - stagnation_trigger = 3: require three consecutive no-change max-energy steps
#   before treating the state as stuck enough to snapshot.
# - energy_hist_len = 8: keep a short recent max-energy window for oscillation checks.
# - oscillation_trigger = 4: with the updated detector, four revisits inside that
#   window is enough to flag a repeating pattern.

code = IRISC_P_dv3_R050_L54_N1296
decoder_type = ml
collect = 1

candidate_k = 16
feature_list = energy,flip_impact,flip_count

ml_trigger_mode = state_based
energy_hist_len = 8
stagnation_trigger = 4
oscillation_trigger = 6

