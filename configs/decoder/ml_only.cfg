# ML-only mode: uses ML local escapes without feedback-shift rounds.

# Code under codes/
code = IRISC_P_dv3_R050_L54_N1296

# Pure ML decoder path.
decoder_type = ml
collect = 0

# Keep candidate/feature contract aligned with current training/data setup.
candidate_selection = max_energy_checks
candidate_k = 16
feature_list = energy,flip_impact,flip_count

# Labeling is ignored in inference when collect=0, but kept for clarity.
labeling_strategy = corrective_mask
rollout_iters = 100
rollout_target_flip_count = 0

# ML invocation policy.
ml_invoke_only_if_baseline_fails = 0
ml_periodic_interval = 5

# Rollback guard for harmful ML actions.
allowed_worsening = 4

# Stagnation detection for stuck-triggered ML path.
energy_hist_len = 8
stagnation_trigger = 2
oscillation_trigger = 6

# Shared baseline control (used by pgdbf path and shared perturbation logic).
pgdbf_flip_probability = 0.70
